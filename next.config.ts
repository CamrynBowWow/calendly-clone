import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
	/* config options here */
	/* next.js 14 issue but not in next.js 15 */
	// experimental: {
	// 	staleTimes: {
	// 		dynamic: 0,
	// 	},
	// },
};

export default nextConfig;
